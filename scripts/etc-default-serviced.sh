#!/bin/bash

# zenoss-inspector-tags serviced serviced-worker ha verify

#
# Capture the complete contents of the configuration so we can see
# any comments the admin might have left for themselves.
cat /etc/default/serviced
